import { MyScrollDirective } from './my-scroll.directive';

describe('MyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
